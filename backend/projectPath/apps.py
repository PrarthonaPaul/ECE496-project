# projectPath/apps.py
from django.apps import AppConfig

class ProjectPathConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'projectPath'  # This should match the name you added to INSTALLED_APPS
